# radio-silence
It blocks web APIs that could expose SDR/radio-adjacent hardware or leak device/browser communication surfaces: WebUSB, Web Serial, WebHID, Web Bluetooth, WebRTC, and Media Devices. The extension has a popup UI, per-tab block counter, activity log, badge updates, and persistent shield toggles.
